# lightrag/requirements.txt
# Minimal dependencies for ICE LightRAG integration

# Core LightRAG (v1.4.9+ required for QueryResult with references)
lightrag-hku>=1.4.9

# For integration with existing Streamlit app
streamlit
pandas

# Environment management
python-dotenv
